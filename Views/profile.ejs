<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("partials/header") %>
    <title>Login</title>
  </head>
  <body>
    <%- include ("partials/nav") %>
    <h1>Welcome to <%= user.name %>'s page.</h1>
    <img src="<%= user.thumbnail %>" alt="" />
    <p><%= user.googleID %></p>
    <p><%= user.date %></p>

    <% if (posts.length > 0) { %> <% posts.sort((a, b) => { var c = new
    Date(a.date); var d = new Date(b.date); return d-c;} ) %> <%
    console.log("posts11",posts) %> <% for (let i = 0; i < posts.length; i++) {
    %>
    <div class="card" style="width: 18rem">
      <div class="card-body">
        <h5 class="card-title"><%= posts[i].title %></h5>
        <p class="card-text"><%= posts[i].content %></p>
        <a href="#" class="btn btn-primary"><%= posts[i].date %> </a>
      </div>
    </div>
    <% } %> <% } %>
  </body>
</html>
